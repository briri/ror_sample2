<h1>Login</h1>

<%= form_for(:user, :url => users_path, :method => 'patch', :data ) do |f| %>
    <p>
      <%= f.label :login %><br />
      <%= f.text_field :login %>
    </p>
    <p>
      <%= f.label :password %><br />
      <%= f.text_field :password %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>